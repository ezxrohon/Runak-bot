pyrogram==2.0.106
tgcrypto==1.2.5
httpx==0.27.0
python-dotenv==1.0.1

# ---- Music (voice chat) - only used if SESSION_STRING is set in .env.
# These are heavier deps (ffmpeg bindings, browser-like scraping for
# yt-dlp) - if you don't want the /play feature at all you can delete
# this block, just leave SESSION_STRING blank in .env either way.
py-tgcalls>=2.2.0
py-yt-search
yt-dlp[default]
